#ifndef DEFAULTS_H
#define DEFAULTS_H

#define DEFAULT_BLOCK_SIZE   128   // bytes, must be divisible by 8 (32 bits) for indirect addressing
#define DEFAULT_TOTAL_BLOCKS 8192  // excluding the superblock
#define DEFAULT_TOTAL_INODES 1024

#endif
