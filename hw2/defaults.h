#ifndef DEFAULTS_H
#define DEFAULTS_H

#include <stdlib.h>
#include <stdint.h>

const size_t   DEFAULT_BOOT_OFFSET  = 1024;
const uint16_t DEFAULT_MAGIC        = 0xEF53;
const uint32_t DEFAULT_BLOCK_SIZE   = 0;
const uint32_t DEFAULT_TOTAL_BLOCKS = 0;
const uint32_t DEFAULT_INODE_SIZE   = 0;
const uint32_t DEFAULT_TOTAL_INODES = 0;

#endif
